# Used only by local CRE simulation. Do not commit .env.
# Insert a real Anthropic key only on your local machine or in CRE's secret manager.
ANTHROPIC_API_KEY=
# RSA-2048 PKCS#8 private key for `keyId: v1`; use a local ignored .env only.
# Never expose this value to the browser or commit it.
SUMMARY_RSA_PRIVATE_V1_PEM=
